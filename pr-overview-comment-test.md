# PR overview comment verification

This temporary change verifies that the dev reviewer posts the generated PR summary as a PR-level comment.

Expected result:

- The comment contains a `Generated description` heading.
- The merge verdict appears in the same comment.
